import { Box } from "@mui/material";

export const Home = () => {
  return (
    <Box sx={{ mt: { sm: "var(--fixed-ui-spacing)" } }}>
      "last played track; most played tracks; recently added track; etc";
    </Box>
  );
};
