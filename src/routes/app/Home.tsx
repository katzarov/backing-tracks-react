import { Box } from "@mui/material";

export const Home = () => {
  return (
    <Box sx={{ mt: { sm: "4rem" } }}>
      "last played track; most played tracks; recently added track; etc";
    </Box>
  );
};
